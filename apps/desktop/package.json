{
  "name": "@rss-mdm-agent/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "tauri dev",
    "dev:web": "pnpm --filter @rss-mdm-agent/ui build && vite --host 127.0.0.1",
    "build": "vue-tsc --noEmit -p tsconfig.json && vite build",
    "desktop:build": "tauri build --no-bundle"
  },
  "dependencies": {
    "@rss-mdm-agent/ui": "workspace:*",
    "vue": "3.5.38"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.2"
  }
}
