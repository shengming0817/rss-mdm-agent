# execution-interaction

一次性交互状态机，以有界引用、命令和调用方可信时间计算待提交转换，无系统时钟、授权或执行副作用。

用户确认、隐私同意与管理员批准分别建模；宿主验证响应者和引用，不能凭回答类型授予权限。过期绝不默认为同意；重复命令保留原结果，内容冲突拒绝，终态后的迟到命令不能改写事实。取消本交互不取消设备任务。

持久层在受保护主体命名空间内条件更新，冲突后重新读取并按当前可信时间计算。快照合法不证明存储真实。创建等待时预留终态空间，避免无法保存回答；具体边界和 API 见 [src](src/)。
